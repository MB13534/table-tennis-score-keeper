// theme.js

import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#181717", // Replace with your desired primary color
    },
    secondary: {
      main: "#a78d00", // Replace with your desired secondary color
    },
    // Add other colors here if needed
  },
  // Add other theme properties like typography, spacing, etc., if needed
});

export default theme;
